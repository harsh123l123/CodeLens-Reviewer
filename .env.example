# Optional: Google Gemini API Key for deep LLM semantic review
# If left empty or omitted, CodeLens automatically uses the built-in static & security engine.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional server configuration
HOST=127.0.0.1
PORT=8000
